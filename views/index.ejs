<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>  
    <div class="box" id='heading'>
        <h1 style="color: black;"><%=day%></h1>
    </div>
      <div class="box">
        <% for (var i=0; i<newItem.length; i++){%>
                <div class="item">
                    <input type="checkbox">
            <p> <%=newItem[i]%> </p>
                </div>
       <% }%>
       <form action="/" class='item' method="POST">
        <input type="text" name = 'list' placeholder="Enter your item" autocomplete="off">
        <button type='submit' name='btn' value=<%= day %> >+</button>
    </form>
      </div>
  
</body>
</html>